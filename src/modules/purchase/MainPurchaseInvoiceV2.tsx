import SearchableSelect from "@/components/SearchableSelect";
import { useCallback, useEffect, useMemo, useState } from "react";
import { ArrowLeft, Eye, Plus, Printer, Save, Trash2 } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { ErrorBanner, Modal, PageHeader, formatCurrency } from "@/components/ui";
import PrintLayout from "@/components/PrintLayout";
import { calculateConfiguredChargeAmount, chargeQuantityForUnit, type ConfiguredChargeUnit } from "@/lib/chargeCalculation";
import { triggerPrint } from "@/lib/exportUtils";
import { supabase } from "@/lib/supabase";

type InvoiceType = "Purchase Invoice" | "Tax Invoice";
type Supplier = { id: string; name: string; name_urdu?: string | null; address?: string | null; phone?: string | null; email?: string | null; ntn?: string | null; strn?: string | null };
type Item = { id: string; name: string; name_urdu?: string | null; sku?: string | null; cost?: number | string | null; unit?: string | null; grade?: string | null; size?: string | null };
type Godown = { id: string; name: string; name_urdu?: string | null };
type PurchaseLine = { item_id: string; description: string; godown_id: string; qty: string; unit_cost: string; tax_percent: string };
type ConsolidatedOption = { id: string; invoice_no: string; invoice_date: string; supplier_id: string; supplier_name: string; reference_name?: string | null; reference_no: string | null; reference_notes?: string | null; total: number | string; linked_purchase_order_id: string | null; invoice_type: InvoiceType };
type ConfiguredCharge = { charge_key: string; charge_name: string; default_rate: number | string; is_fixed: boolean; tax_applicable: boolean; purchase_treatment?: "landed_cost" | "expense" | null; unit: ConfiguredChargeUnit; cost_account_id?: string | null };
type ChargeRow = { charge_key: string; quantity: string; rate: string; amount: string; tax_percent: string };
type CompanyPrintSettings = { company_name?: string | null; address?: string | null; phone?: string | null; email?: string | null; ntn?: string | null; strn?: string | null; logo_url?: string | null; document_header?: string | null; document_header_urdu?: string | null; document_footer?: string | null; document_footer_urdu?: string | null; prepared_by_label?: string | null; checked_by_label?: string | null; approved_by_label?: string | null };
type SupplierSnapshot = { currentOutstanding: number; lastPaymentDate: string | null; lastPaymentAmount: number; paidToday: number };

const emptyLine = (tax = "0", godown = ""): PurchaseLine => ({ item_id: "", description: "", godown_id: godown, qty: "1", unit_cost: "0", tax_percent: tax });
const treatmentOf = (charge: ConfiguredCharge) => charge.purchase_treatment || "landed_cost";
const unitLabel = (unit: ConfiguredChargeUnit) => ({ fixed: "Fixed", percent: "%", per_qty: "per qty", per_kg: "per kg", per_ton: "per ton", per_piece: "per piece", manual: "manual" }[unit] || unit);

export default function MainPurchaseInvoiceV2() {
  const navigate = useNavigate();
  const [suppliers, setSuppliers] = useState<Supplier[]>([]); const [items, setItems] = useState<Item[]>([]); const [godowns, setGodowns] = useState<Godown[]>([]);
  const [configuredCharges, setConfiguredCharges] = useState<ConfiguredCharge[]>([]); const [charges, setCharges] = useState<ChargeRow[]>([]); const [chargeToAdd, setChargeToAdd] = useState("");
  const [companyPrint, setCompanyPrint] = useState<CompanyPrintSettings>({}); const [orderNo, setOrderNo] = useState(""); const [supplierId, setSupplierId] = useState("");
  const [orderDate, setOrderDate] = useState(new Date().toISOString().slice(0, 10)); const [invoiceType, setInvoiceType] = useState<InvoiceType>("Purchase Invoice"); const [configuredTaxRate, setConfiguredTaxRate] = useState<string | null>(null);
  const [discountAmount,setDiscountAmount]=useState("0"); const [rows, setRows] = useState<PurchaseLine[]>([emptyLine()]); const [allConsolidated, setAllConsolidated] = useState<ConsolidatedOption[]>([]); const [selectedConsolidatedIds, setSelectedConsolidatedIds] = useState<string[]>([]); const [consolidatedSearch, setConsolidatedSearch] = useState("");
  const [supplierSnapshot, setSupplierSnapshot] = useState<SupplierSnapshot | null>(null); const [snapshotLoading, setSnapshotLoading] = useState(false); const [previewOpen, setPreviewOpen] = useState(false); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null);
  const globalTaxPercent = invoiceType === "Tax Invoice" ? configuredTaxRate || "0" : "0";

  const loadBase = useCallback(async () => { setLoading(true); setError(null); try { const [supplierRes,itemRes,godownRes,orderNoRes,taxRes,chargeRes,consolidatedRes,companyRes] = await Promise.all([
    supabase.from("suppliers").select("*").eq("is_active",true).order("name"), supabase.from("items").select("*").order("name"), supabase.from("godowns").select("id,name,name_urdu").order("name"), supabase.rpc("next_purchase_order_no"),
    supabase.from("tax_rates").select("rate").eq("is_active",true).eq("is_fixed",true).in("applies_to",["purchase","both"]).order("created_at").limit(1).maybeSingle(),
    supabase.from("charge_master").select("charge_key,charge_name,default_rate,is_fixed,tax_applicable,purchase_treatment,unit,cost_account_id").eq("is_active",true).in("applies_to",["purchase","both"]).order("charge_name"),
    supabase.rpc("get_available_consolidated_purchase_invoices_v2",{p_supplier_id:null,p_order_id:null}), supabase.from("company_settings").select("*").maybeSingle()]);
    const firstError=[supplierRes.error,itemRes.error,godownRes.error,orderNoRes.error,taxRes.error,chargeRes.error,consolidatedRes.error,companyRes.error].find(Boolean); if(firstError) throw firstError;
    const loadedGodowns=(godownRes.data??[]) as Godown[]; const tax=taxRes.data?String(Number(taxRes.data.rate)||0):null;
    setSuppliers((supplierRes.data??[]) as Supplier[]); setItems((itemRes.data??[]) as Item[]); setGodowns(loadedGodowns); setOrderNo(String(orderNoRes.data??"")); setConfiguredTaxRate(tax); setConfiguredCharges((chargeRes.data??[]) as ConfiguredCharge[]); setAllConsolidated((consolidatedRes.data??[]) as ConsolidatedOption[]); setCompanyPrint((companyRes.data||{}) as CompanyPrintSettings); setRows([emptyLine("0",loadedGodowns[0]?.id||"")]);
  } catch(e:any){setError(e?.message||"Failed to load Main Purchase Invoice.");} finally{setLoading(false);} },[]); useEffect(()=>{void loadBase();},[loadBase]);

  useEffect(()=>{ if(!supplierId){setSupplierSnapshot(null);return;} void(async()=>{setSnapshotLoading(true); const [ordersRes,paymentsRes]=await Promise.all([supabase.from("purchase_orders").select("outstanding_amount,status").eq("supplier_id",supplierId).eq("status","posted"),supabase.from("purchase_payment_allocations").select("allocation_date,amount,created_at").eq("supplier_id",supplierId).order("allocation_date",{ascending:false}).order("created_at",{ascending:false})]); setSnapshotLoading(false); if(ordersRes.error||paymentsRes.error)return; const payments=paymentsRes.data??[]; const key=new Date().toISOString().slice(0,10); setSupplierSnapshot({currentOutstanding:(ordersRes.data??[]).reduce((s:number,r:any)=>s+Math.max(0,Number(r.outstanding_amount)||0),0),lastPaymentDate:payments[0]?.allocation_date?String(payments[0].allocation_date):null,lastPaymentAmount:Number(payments[0]?.amount)||0,paidToday:payments.reduce((s:number,r:any)=>String(r.allocation_date).slice(0,10)===key?s+(Number(r.amount)||0):s,0)});})(); },[supplierId]);

  const directSubtotal=rows.reduce((s,r)=>s+(Number(r.qty)||0)*(Number(r.unit_cost)||0),0); const directItemTax=invoiceType==="Tax Invoice"?rows.reduce((s,r)=>{const b=(Number(r.qty)||0)*(Number(r.unit_cost)||0);return s+b*(Number(r.tax_percent)||0)/100;},0):0;
  const recalculateCharge=useCallback((charge:ChargeRow,nextRate?:string)=>{const master=configuredCharges.find(r=>r.charge_key===charge.charge_key);if(!master)return charge;if(master.unit==="manual"){const a=Math.max(0,Number(nextRate??charge.amount??charge.rate)||0);return{...charge,quantity:"1",rate:String(a),amount:String(Number(a.toFixed(2)))};}const rate=Number(nextRate??charge.rate??master.default_rate)||0;const quantity=chargeQuantityForUnit(master.unit,rows,items,directSubtotal);const amount=calculateConfiguredChargeAmount({unit:master.unit,rate,rows,items,baseAmount:directSubtotal});return{...charge,quantity:String(Number(quantity.toFixed(3))),rate:String(rate),amount:String(Number(amount.toFixed(2)))};},[configuredCharges,rows,items,directSubtotal]);
  useEffect(()=>{if(!charges.length)return;setCharges(c=>c.map(ch=>configuredCharges.find(m=>m.charge_key===ch.charge_key)?.unit==="manual"?ch:recalculateCharge(ch)));},[recalculateCharge,configuredCharges]);
  const directCharges=charges.reduce((s,c)=>s+(Number(c.amount)||0),0); const directChargeTax=invoiceType==="Tax Invoice"?charges.reduce((s,c)=>s+((Number(c.amount)||0)*(Number(c.tax_percent)||0))/100,0):0; const landedCharges=charges.reduce((s,c)=>treatmentOf(configuredCharges.find(m=>m.charge_key===c.charge_key)||{} as ConfiguredCharge)==="landed_cost"?s+(Number(c.amount)||0):s,0); const expenseCharges=directCharges-landedCharges;
  const availableConsolidated=useMemo(()=>allConsolidated.filter(i=>(!supplierId||i.supplier_id===supplierId)&&i.invoice_type===invoiceType),[allConsolidated,supplierId,invoiceType]); const visibleConsolidated=useMemo(()=>{const q=consolidatedSearch.trim().toLowerCase();return q?availableConsolidated.filter(i=>[i.invoice_no,i.reference_name,i.reference_no,i.supplier_name,i.invoice_date].some(v=>String(v??"").toLowerCase().includes(q))):availableConsolidated;},[availableConsolidated,consolidatedSearch]); const selectedConsolidated=allConsolidated.filter(i=>selectedConsolidatedIds.includes(i.id)); const consolidatedTotal=selectedConsolidated.reduce((s,i)=>s+Number(i.total||0),0); const grossTotal=directSubtotal+directItemTax+directCharges+directChargeTax+consolidatedTotal; const discountValue=Math.max(0,Number(discountAmount)||0); const grandTotal=Math.max(0,grossTotal-discountValue); const projectedSupplierBalance=(supplierSnapshot?.currentOutstanding||0)+grandTotal; const selectedSupplier=suppliers.find(r=>r.id===supplierId)||null;
  useEffect(()=>{setSelectedConsolidatedIds(c=>c.filter(id=>availableConsolidated.some(i=>i.id===id)));},[availableConsolidated]);
  const updateLine=(index:number,field:keyof PurchaseLine,value:string)=>setRows(c=>c.map((r,i)=>{if(i!==index)return r;const n={...r,[field]:value};if(field==="item_id"){const item=items.find(x=>x.id===value);if(item)n.unit_cost=String(Number(item.cost)||0);}return n;}));
  const changeInvoiceType=(next:InvoiceType)=>{setInvoiceType(next);const tax=next==="Tax Invoice"?configuredTaxRate||"0":"0";setRows(c=>c.map(r=>({...r,tax_percent:tax})));setCharges(c=>c.map(ch=>({...ch,tax_percent:next==="Tax Invoice"&&configuredCharges.find(m=>m.charge_key===ch.charge_key)?.tax_applicable?tax:"0"})));};
  const addCharge=()=>{if(!chargeToAdd||charges.some(r=>r.charge_key===chargeToAdd))return;const master=configuredCharges.find(r=>r.charge_key===chargeToAdd);if(!master)return;const base:ChargeRow={charge_key:master.charge_key,quantity:"1",rate:master.unit==="manual"?"0":String(Number(master.default_rate)||0),amount:"0",tax_percent:invoiceType==="Tax Invoice"&&master.tax_applicable?configuredTaxRate||"0":"0"};setCharges(c=>[...c,recalculateCharge(base)]);setChargeToAdd("");};
  const toggleConsolidated=(invoice:ConsolidatedOption,checked:boolean)=>{setError(null);if(checked){if(!supplierId)setSupplierId(invoice.supplier_id);if(supplierId&&supplierId!==invoice.supplier_id)return setError("Main Purchase Invoice and Consolidated Purchase must use the same supplier. / مین خریداری انوائس اور کنسولیڈیٹڈ خریداری میں ایک ہی سپلائر ہونا ضروری ہے۔");setSelectedConsolidatedIds(c=>c.includes(invoice.id)?c:[...c,invoice.id]);}else setSelectedConsolidatedIds(c=>c.filter(id=>id!==invoice.id));};

  const handleSave=async(event:React.FormEvent)=>{event.preventDefault();setError(null);if(!supplierId)return setError("Select a supplier or a Consolidated Purchase document.");if(invoiceType==="Tax Invoice"&&!configuredTaxRate)return setError("Configure one active fixed Purchase/Both tax rate before using With Tax.");if(discountValue>grossTotal)return setError("Discount cannot exceed the invoice gross total.");const validRows=rows.filter(r=>r.item_id&&Number(r.qty)>0);if(!validRows.length&&!selectedConsolidatedIds.length)return setError("Add at least one direct item or link a posted Consolidated Purchase document.");if(validRows.some(r=>!r.godown_id))return setError("Select a destination godown for every direct item.");if(selectedConsolidated.some(i=>i.supplier_id!==supplierId||i.invoice_type!==invoiceType))return setError("Linked Consolidated Purchase documents must match supplier and tax type.");setSaving(true);let createdOrderId:string|null=null;try{const{data:order,error:orderError}=await supabase.from("purchase_orders").insert({order_no:orderNo,supplier_id:supplierId,order_date:orderDate,status:"draft",invoice_type:invoiceType,tax_percent:invoiceType==="Tax Invoice"?Number(configuredTaxRate)||0:0,discount_amount:Number(discountValue.toFixed(2)),total:Number(grossTotal.toFixed(2))}).select("id").single();if(orderError)throw orderError;createdOrderId=order.id;if(validRows.length){const{error:lineError}=await supabase.from("purchase_order_lines").insert(validRows.map(r=>({order_id:order.id,item_id:r.item_id,godown_id:r.godown_id,qty:Number(r.qty)||0,unit_cost:Number(r.unit_cost)||0,tax_percent:invoiceType==="Tax Invoice"?Number(r.tax_percent)||0:0,description:r.description.trim()||null,line_total:(Number(r.qty)||0)*(Number(r.unit_cost)||0)})));if(lineError)throw lineError;}const activeCharges=charges.filter(c=>Number(c.amount)>0);if(activeCharges.length){const{error:chargeError}=await supabase.from("purchase_order_charges").insert(activeCharges.map(c=>{const m=configuredCharges.find(r=>r.charge_key===c.charge_key);return{order_id:order.id,charge_key:c.charge_key,charge_label:m?.charge_name||c.charge_key,quantity:Number(c.quantity)||1,rate:Number(c.rate)||0,amount:Number(c.amount)||0,tax_percent:invoiceType==="Tax Invoice"&&m?.tax_applicable?Number(c.tax_percent)||0:0,treatment:m?treatmentOf(m):"landed_cost",cost_account_id:m?.cost_account_id||null};}));if(chargeError)throw chargeError;}const{error:discountError}=await supabase.rpc("upsert_commercial_invoice_discount",{p_document_type:"purchase_main",p_document_no:orderNo,p_mode:"fixed",p_value:Number(discountValue.toFixed(2)),p_amount:Number(discountValue.toFixed(2))});if(discountError)throw discountError;const{error:totalSyncError}=await supabase.from("purchase_orders").update({total:Number(grossTotal.toFixed(2)),discount_amount:Number(discountValue.toFixed(2))}).eq("id",order.id).eq("status","draft");if(totalSyncError)throw totalSyncError;const{error:linkError}=await supabase.rpc("replace_purchase_order_consolidated_invoices",{p_order_id:order.id,p_consolidated_invoice_ids:selectedConsolidatedIds});if(linkError)throw linkError;navigate(`/purchase/${order.id}`);}catch(e:any){if(createdOrderId)await supabase.from("purchase_orders").delete().eq("id",createdOrderId).eq("status","draft");setError(e?.message||"Failed to save Main Purchase Invoice.");}finally{setSaving(false);}};
  const printItems=rows.filter(r=>r.item_id&&Number(r.qty)>0).map(r=>{const item=items.find(c=>c.id===r.item_id);const base=(Number(r.qty)||0)*(Number(r.unit_cost)||0);return{name:item?.name||"—",description:r.description||null,grade:item?.grade||null,size:item?.size||null,qty:Number(r.qty)||0,unitPrice:Number(r.unit_cost)||0,lineTotal:base,taxPercent:invoiceType==="Tax Invoice"?Number(r.tax_percent)||0:undefined,taxAmount:invoiceType==="Tax Invoice"?base*(Number(r.tax_percent)||0)/100:undefined,unit:item?.unit||null};}); const printCharges=charges.filter(r=>Number(r.amount)>0).map(r=>({label:configuredCharges.find(m=>m.charge_key===r.charge_key)?.charge_name||r.charge_key,amount:Number(r.amount)||0}));

  return <div><Link to="/purchase" className="mb-4 inline-flex items-center gap-1 text-sm text-primary-600 hover:text-primary-700"><ArrowLeft className="h-3.5 w-3.5" />Back to Purchase</Link><PageHeader title="Main Purchase Invoice / مین خریداری انوائس" subtitle="Canonical supplier invoice: direct items, dynamic charges and linked Consolidated receipts" />{error&&<ErrorBanner message={error}/>}<form onSubmit={handleSave} className="space-y-4">
  <section className="card p-5"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-semibold text-slate-900">Invoice Information</h3><p className="mt-1 text-xs text-slate-500">Payment mode is separate from invoice classification.</p></div><button type="button" className="btn-secondary" onClick={()=>setPreviewOpen(true)}><Eye className="h-3.5 w-3.5"/>Preview</button></div><div className="grid grid-cols-1 gap-4 md:grid-cols-4"><div><label className="label">Invoice Type</label><SearchableSelect className="input" value={invoiceType} onChange={e=>changeInvoiceType(e.target.value as InvoiceType)}><option value="Purchase Invoice">Without Tax</option><option value="Tax Invoice">With Tax</option></SearchableSelect></div><div><label className="label">Invoice No.</label><input className="input bg-slate-50 font-semibold" readOnly value={orderNo}/></div><div><label className="label">Invoice Date</label><input className="input" type="date" value={orderDate} onChange={e=>setOrderDate(e.target.value)}/></div><div><label className="label">Fixed VAT %</label><input className="input bg-slate-50 text-right" readOnly value={invoiceType==="Tax Invoice"?configuredTaxRate||"Not configured":"0"}/></div><div className="md:col-span-2"><label className="label">Supplier</label><SearchableSelect className="input" value={supplierId} onChange={e=>setSupplierId(e.target.value)}><option value="">— Select supplier —</option>{suppliers.map(s=><option key={s.id} value={s.id}>{s.name}{s.name_urdu?` / ${s.name_urdu}`:""}</option>)}</SearchableSelect></div></div></section>
  <section className="rounded-xl border border-blue-300 bg-blue-50/40 p-4"><div className="mb-3 flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-semibold text-blue-950">Link Consolidated Purchase</h3><p className="mt-1 text-xs text-blue-700">Posted receiving documents are linked here. Their physical stock is not received again.</p></div><Link to="/purchase/consolidated" className="btn-secondary text-sm">Open Consolidated Purchase</Link></div><input className="input mb-3" placeholder="Search invoice, reference or supplier…" value={consolidatedSearch} onChange={e=>setConsolidatedSearch(e.target.value)}/>{loading?<div className="rounded border bg-white p-4 text-center text-sm text-slate-400">Loading…</div>:!visibleConsolidated.length?<div className="rounded border bg-white p-4 text-center text-sm text-slate-500">No posted unused matching Consolidated Purchase documents.</div>:<div className="max-h-64 overflow-y-auto rounded border border-blue-100 bg-white">{visibleConsolidated.map(i=><label key={i.id} className="flex items-center gap-3 border-b border-slate-100 px-3 py-2"><input type="checkbox" checked={selectedConsolidatedIds.includes(i.id)} onChange={e=>toggleConsolidated(i,e.target.checked)}/><div className="min-w-0 flex-1"><div className="font-semibold">{i.invoice_no}</div><div className="text-xs text-slate-500">{i.supplier_name} · {i.invoice_date}{i.reference_no?` · ${i.reference_no}`:""}</div></div><strong>{formatCurrency(Number(i.total)||0)}</strong></label>)}</div>}<div className="mt-3 flex justify-between rounded bg-white px-3 py-2 text-sm"><span>Selected: <strong>{selectedConsolidatedIds.length}</strong></span><span className="text-blue-700">Linked Total: <strong>{formatCurrency(consolidatedTotal)}</strong></span></div></section>
  {supplierId&&<section className="card overflow-hidden"><div className="border-b px-4 py-3"><h3 className="font-semibold">Supplier Financial Position</h3></div><div className="grid grid-cols-2 md:grid-cols-5"><div className="border-r p-3"><div className="text-xs text-slate-500">Current AP</div><strong className="text-amber-700">{snapshotLoading?"…":formatCurrency(supplierSnapshot?.currentOutstanding||0)}</strong></div><div className="border-r p-3"><div className="text-xs text-slate-500">Last Payment</div><strong className="text-emerald-700">{formatCurrency(supplierSnapshot?.lastPaymentAmount||0)}</strong><div className="text-xs text-slate-400">{supplierSnapshot?.lastPaymentDate||"—"}</div></div><div className="border-r p-3"><div className="text-xs text-slate-500">Paid Today</div><strong className="text-blue-700">{formatCurrency(supplierSnapshot?.paidToday||0)}</strong></div><div className="border-r p-3"><div className="text-xs text-slate-500">Invoice Balance</div><strong>{formatCurrency(grandTotal)}</strong></div><div className="p-3"><div className="text-xs text-slate-500">Projected AP</div><strong className="text-rose-700">{formatCurrency(projectedSupplierBalance)}</strong></div></div></section>}
  <section className="card mx-auto w-[calc(100%-2rem)] max-w-[1480px] p-5"><div className="mb-3 flex items-center justify-between"><div><h3 className="font-semibold">Direct Purchase Items</h3><p className="text-xs text-slate-500">Direct item stock posts only when the Main Purchase Invoice is posted.</p></div><button type="button" className="btn-secondary" onClick={()=>setRows(c=>[...c,emptyLine(globalTaxPercent,godowns[0]?.id||"")])}><Plus className="h-3.5 w-3.5"/>Add Row</button></div><div className="rounded border"><table className="w-full table-fixed text-sm"><thead className="bg-slate-50"><tr><th className="w-[18%] p-2 text-left">Item</th><th className="w-[20%] p-2 text-left">Description</th><th className="w-[15%] p-2 text-left">Godown</th><th className="w-[10%] p-2 text-right">Qty</th><th className="w-[13%] p-2 text-right">Unit Cost</th>{invoiceType==="Tax Invoice"&&<th className="w-[9%] p-2 text-right">VAT %</th>}<th className="w-[12%] p-2 text-right">Amount</th><th className="w-10"/></tr></thead><tbody>{rows.map((r,index)=>{const base=(Number(r.qty)||0)*(Number(r.unit_cost)||0);const vat=invoiceType==="Tax Invoice"?base*(Number(r.tax_percent)||0)/100:0;return <tr key={index} className="border-t"><td className="p-2"><SearchableSelect className="input" value={r.item_id} onChange={e=>updateLine(index,"item_id",e.target.value)}><option value="">— Select item —</option>{items.map(item=><option key={item.id} value={item.id}>{item.name}{item.sku?` · ${item.sku}`:""}</option>)}</SearchableSelect></td><td className="p-2"><input className="input" value={r.description} onChange={e=>updateLine(index,"description",e.target.value)}/></td><td className="p-2"><SearchableSelect className="input" value={r.godown_id} onChange={e=>updateLine(index,"godown_id",e.target.value)}><option value="">— Select —</option>{godowns.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</SearchableSelect></td><td className="p-2"><input className="input text-right" type="number" min="0.001" step="0.001" value={r.qty} onChange={e=>updateLine(index,"qty",e.target.value)}/></td><td className="p-2"><input className="input text-right" type="number" min="0" step="0.01" value={r.unit_cost} onChange={e=>updateLine(index,"unit_cost",e.target.value)}/></td>{invoiceType==="Tax Invoice"&&<td className="p-2"><input className="input bg-slate-50 text-right" readOnly value={r.tax_percent}/></td>}<td className="p-2 text-right font-semibold">{formatCurrency(base+vat)}</td><td className="p-2">{rows.length>1&&<button type="button" className="text-rose-600" onClick={()=>setRows(c=>c.filter((_,i)=>i!==index))}><Trash2 className="h-3.5 w-3.5"/></button>}</td></tr>;})}</tbody></table></div></section>
  <section className="card mx-auto w-[calc(100%-2rem)] max-w-[1480px] p-5"><div className="mb-3 flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-semibold">Applicable Charges</h3><p className="text-xs text-slate-500">One engine for Fixed, Qty, Kg, Ton, %, Piece and Manual. Manual remains editable; other bases recalculate automatically.</p></div><Link to="/sales/charges" className="btn-secondary text-sm">Charge Master</Link></div><div className="flex gap-2"><SearchableSelect className="input max-w-sm" value={chargeToAdd} onChange={e=>setChargeToAdd(e.target.value)}><option value="">— Select charge —</option>{configuredCharges.filter(m=>!charges.some(r=>r.charge_key===m.charge_key)).map(m=><option key={m.charge_key} value={m.charge_key}>{m.charge_name}</option>)}</SearchableSelect><button type="button" className="btn-secondary" onClick={addCharge} disabled={!chargeToAdd}><Plus className="h-3.5 w-3.5"/>Add</button></div><div className="mt-3 grid gap-2 md:grid-cols-2">{!charges.length?<div className="text-sm text-slate-400">No additional charges selected.</div>:charges.map((c,index)=>{const m=configuredCharges.find(r=>r.charge_key===c.charge_key);const manual=m?.unit==="manual";return <div key={c.charge_key} className="rounded border bg-slate-50 p-3"><div className="mb-2 flex justify-between"><div><strong>{m?.charge_name||c.charge_key}</strong><div className="text-xs text-slate-500">{unitLabel(m?.unit||"fixed")} · {m?treatmentOf(m):"landed_cost"}</div></div><button type="button" className="text-rose-600" onClick={()=>setCharges(cur=>cur.filter((_,i)=>i!==index))}><Trash2 className="h-3.5 w-3.5"/></button></div><div className="grid grid-cols-2 gap-2"><div><label className="label">Qty / Basis</label><input className="input bg-slate-100 text-right" readOnly value={c.quantity}/></div><div><label className="label">{manual?"Manual Amount":"Rate"}</label><input className="input text-right" type="number" min="0" step="0.01" disabled={Boolean(m?.is_fixed)} value={c.rate} onChange={e=>setCharges(cur=>cur.map((x,i)=>i===index?recalculateCharge(x,e.target.value):x))}/></div><div><label className="label">Calculated Amount</label><input className="input bg-slate-100 text-right" readOnly value={c.amount}/></div>{invoiceType==="Tax Invoice"&&m?.tax_applicable&&<div><label className="label">VAT %</label><input className="input bg-slate-100 text-right" readOnly value={c.tax_percent}/></div>}</div></div>;})}</div></section>
  <section className="card mx-auto w-[calc(100%-2rem)] max-w-[1480px] p-5"><div className="grid gap-5 lg:grid-cols-[1fr_420px]"><div><h3 className="font-semibold">Review & Save</h3><p className="mt-1 text-sm text-slate-500">Saving creates a draft only. Posting remains a separate controlled action from invoice detail.</p><div className="mt-4 max-w-xs"><label className="label">Discount / رعایت</label><input className="input text-right" type="number" min="0" step="0.01" value={discountAmount} onChange={e=>setDiscountAmount(e.target.value)}/><p className="mt-1 text-[11px] text-slate-400">Main Purchase Invoice only.</p></div></div><div className="rounded-xl bg-slate-50 p-4 text-sm"><div className="flex justify-between py-1"><span>Direct Items</span><span>{formatCurrency(directSubtotal)}</span></div>{invoiceType==="Tax Invoice"&&<div className="flex justify-between py-1"><span>Items VAT</span><span>{formatCurrency(directItemTax)}</span></div>}<div className="flex justify-between py-1"><span>Landed Cost Charges</span><span>{formatCurrency(landedCharges)}</span></div><div className="flex justify-between py-1"><span>Expense Charges</span><span>{formatCurrency(expenseCharges)}</span></div>{invoiceType==="Tax Invoice"&&<div className="flex justify-between py-1"><span>Charge VAT</span><span>{formatCurrency(directChargeTax)}</span></div>}<div className="flex justify-between py-1 text-blue-700"><span>Linked Consolidated</span><span>{formatCurrency(consolidatedTotal)}</span></div><div className="flex justify-between py-1 text-rose-700"><span>Discount</span><strong>- {formatCurrency(discountValue)}</strong></div><div className="mt-2 flex justify-between border-t pt-3 text-lg font-bold"><span>Grand Total</span><span>{formatCurrency(grandTotal)}</span></div></div></div><div className="mt-4 flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setPreviewOpen(true)}><Eye className="h-3.5 w-3.5"/>Preview</button><button className="btn-primary" disabled={saving||loading}><Save className="h-3.5 w-3.5"/>{saving?"Saving…":"Save Draft Purchase Invoice"}</button></div></section>
  </form><Modal open={previewOpen} title={`Purchase Invoice Preview — ${orderNo}`} onClose={()=>setPreviewOpen(false)}><div className="p-2"><PrintLayout voucherTitle={invoiceType==="Tax Invoice"?"Purchase Tax Invoice":"Purchase Invoice"} voucherNo={orderNo} voucherDate={orderDate} company={{name:companyPrint.company_name||"NAVILO",address:companyPrint.address||undefined,phone:companyPrint.phone||undefined,email:companyPrint.email||undefined,taxId:[companyPrint.ntn?`NTN: ${companyPrint.ntn}`:"",companyPrint.strn?`STRN: ${companyPrint.strn}`:""].filter(Boolean).join(" | ")||undefined,logoUrl:companyPrint.logo_url||undefined}} party={{name:selectedSupplier?.name||"—",address:selectedSupplier?.address,phone:selectedSupplier?.phone,email:selectedSupplier?.email,ntn:selectedSupplier?.ntn,strn:selectedSupplier?.strn}} items={printItems} chargeBreakdown={printCharges} itemsTotal={directSubtotal} chargesTotal={directCharges} taxAmount={directItemTax+directChargeTax} showTaxSummary={invoiceType==="Tax Invoice"} normalInvoiceTotal={directSubtotal+directItemTax+directCharges+directChargeTax} hawalaDocuments={selectedConsolidated.map(r=>({id:r.id,invoiceNo:r.invoice_no,invoiceDate:r.invoice_date,referenceName:r.reference_name,referenceNo:r.reference_no,referenceNotes:r.reference_notes,amount:Number(r.total)||0}))} grandTotal={grandTotal} extraFields={discountValue>0?[{label:"Discount / رعایت",value:formatCurrency(discountValue)}]:undefined} documentHeader={companyPrint.document_header||undefined} documentHeaderUrdu={companyPrint.document_header_urdu||undefined} documentFooter={companyPrint.document_footer||undefined} documentFooterUrdu={companyPrint.document_footer_urdu||undefined} documentNotice="DRAFT PURCHASE INVOICE — STOCK/AP/VAT POST ONLY AFTER APPROVAL" documentNoticeUrdu="ڈرافٹ خریداری انوائس — اسٹاک، واجبات اور ٹیکس منظوری کے بعد پوسٹ ہوں گے" signatureLabels={[companyPrint.prepared_by_label||"Prepared By",companyPrint.checked_by_label||"Checked By",companyPrint.approved_by_label||"Approved By"]}/><div className="mt-3 flex justify-end gap-2 border-t pt-3"><button type="button" className="btn-secondary" onClick={()=>triggerPrint(".print-document")}><Printer className="h-3.5 w-3.5"/>Print</button><button type="button" className="btn-primary" onClick={()=>setPreviewOpen(false)}>Close</button></div></div></Modal></div>;
}
